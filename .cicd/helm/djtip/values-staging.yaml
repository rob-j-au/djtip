# Staging environment values for djtip
# Doubles the resources compared to development

# Web pods - 2x development
replicaCount: 4

image:
  repository: robj/djtip
  pullPolicy: Always
  tag: "latest"

imagePullSecrets:
  - name: dockerhub-secret

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}
podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: "haproxy"
  annotations:
    ingress.class: haproxy
  hosts:
    - host: djtip-staging.minikube.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: djtip-staging-tls
      hosts:
        - djtip-staging.minikube.local

# Web pod resources - 2x development
resources:
  requests:
    memory: "512Mi"
    cpu: "200m"
  limits:
    memory: "1Gi"
    cpu: "1000m"

livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

volumes: []
volumeMounts: []

nodeSelector: {}
tolerations: []
affinity: {}

# Sidekiq worker configuration - 2x development
worker:
  replicaCount: 2
  resources:
    requests:
      memory: "512Mi"
      cpu: "200m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

metrics:
  enabled: true

# MongoDB configuration - 2x development
mongodb:
  enabled: true
  auth:
    enabled: false
  architecture: standalone
  persistence:
    enabled: true  # Enable persistence for staging
    size: 8Gi
  resources:
    requests:
      memory: "512Mi"
      cpu: "200m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

# Redis configuration - 2x development
redis:
  enabled: true
  auth:
    enabled: false
  architecture: standalone
  master:
    persistence:
      enabled: true  # Enable persistence for staging
      size: 4Gi
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "400m"
