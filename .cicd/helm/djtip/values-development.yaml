# Development environment overrides
# Inherits from values.yaml

# Development-specific ingress
ingress:
  hosts:
    - host: app.dev.djtip.jennings.au
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: djtip-tls
      hosts:
        - app.dev.djtip.jennings.au

# Development uses default env values from values.yaml
# No environment variable overrides needed
