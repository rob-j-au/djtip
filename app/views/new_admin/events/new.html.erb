<% content_for :breadcrumbs do %>
  <li><%= link_to "Events", new_admin_events_path %></li>
  <li>New Event</li>
<% end %>

<div class="card bg-base-100 shadow-xl">
  <div class="card-body">
    <div class="flex items-center mb-6">
      <div class="w-12 h-12 bg-primary/10 rounded-lg flex items-center justify-center mr-4">
        <i class="fas fa-calendar-plus text-primary text-xl"></i>
      </div>
      <div>
        <h2 class="card-title text-2xl">Create New Event</h2>
        <p class="text-base-content/70">Add a new event to your djtip platform</p>
      </div>
    </div>

    <%= render 'form' %>
  </div>
</div>
